import { ConfigData } from './types';
export declare class Config {
    private store;
    init(config: ConfigData): this;
    update(config: Partial<ConfigData>): this;
    private set;
    get(): ConfigData;
}
